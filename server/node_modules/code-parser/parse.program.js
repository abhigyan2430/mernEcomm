module.exports = (program, parent) => {
            scaffold.programs.push({
                name: fileName
            })
            program.forEach(node => {
                if (node.type == types.class) {
                    iterateClass(node, fileName);
                    return;
                }
            })
        };